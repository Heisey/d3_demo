import configGraphSize from "./configGraphSize";
import configMargin from "./configMargin";
import configXScale from "./configXScale";
import configYScale from "./configYScale";

import generateGraphGroup from './generateGraphGroup'

import renderGraph  from "./renderGraph";

const helpers = {
  configGraphSize,
  configMargin,
  configXScale,
  configYScale,
  generateGraphGroup,
  renderGraph
}

export default helpers